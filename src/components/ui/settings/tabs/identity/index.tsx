import type { Component } from "solid-js";

const serverIdentityModule: Component = () => {
    return (
        <div>
            serverIdentityModule
        </div>
    )
}

export default serverIdentityModule;